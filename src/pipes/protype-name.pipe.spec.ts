import { ProtypeNamePipe } from './protype-name.pipe';

describe('ProtypeNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ProtypeNamePipe();
    expect(pipe).toBeTruthy();
  });
});
